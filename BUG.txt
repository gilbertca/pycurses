Issue with halign not having programmed width
Issue with _map_colors and single colors
Issue where new arbitrary color pairs need to be generated due to curses changing all color pair definitions
Issue where not including all required color attributes in JSON will cause NoneType crash
Issue where tab not working at first press:
	Recreate: Tab does not work unless a write function is called first.
